name: mnemonic_backup_flow
description: Recovery-phrase reveal, verification and import screens for Flutter wallets, with a PIN gate, screenshot-protection hooks, random-word checks and clipboard clearing.
version: 0.1.0
repository: https://github.com/globalmpc/rwa-kit-flutter
issue_tracker: https://github.com/globalmpc/rwa-kit-flutter/issues
topics:
  - wallet
  - bip39
  - mnemonic
  - security
  - rwa

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
  bip39_mnemonic: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
